import React from 'react';
import styles from './SubTitle.module.css';

const SubTitle = () => {
  return <h2 className={styles.subtitle}>This is an example component with ReactJS</h2>;
};

export default SubTitle;
